/**
 * Content script for the extension
 */

// Run in the same environment as the user's page (current page)
// Use to modify UI, add new buttons, add new features using JS, CSS to manipulate the DOM
